import classes from "./ListsPage.module.css";

const ListsPage = () => {
    return (
        <div className={classes.mainContainer}>
            <p>Lists</p>
        </div>
    );
};

export default ListsPage;
